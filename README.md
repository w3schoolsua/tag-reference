# 📘 HTML Elements Reference
Повністю автономний інтерактивний довідник HTML‑елементів (HTML namespace), який генерує таблицю з JSON‑файлу та підтримує темну/світлу тему, сортування, пошук і фільтри.

## 📌 Опис проєкту
Цей проєкт — це статичний веб‑довідник, який містить:
- повний список HTML‑елементів (standard, obsolete, historical);
- категорії для кожного елемента;
- короткі описи;
- автогенерацію таблиці з JSON;
- сортування по кліку на заголовки;
- пошук по тегу, опису та категорії;
- фільтри за статусом;
- перемикач теми (світла/темна), що зберігається у localStorage.
Проєкт не використовує жодних фреймворків — лише HTML + CSS + JavaScript + JSON.

## 📂 Структура репозиторію
/
├── index.html          # Головна сторінка довідника
├── styles.css          # Стилі інтерфейсу та таблиці
├── script.js           # Логіка автогенерації, фільтрів, сортування та теми
└── html-elements.json  # Повний список HTML‑елементів

## 🚀 Як запустити
- Клонуйте репозиторій:
git clone https://github.com/your-username/html-elements-reference.git

- Відкрийте файл: index.html у будь‑якому браузері. Ніякого сервера не потрібно — усе працює локально.

## ✨ Можливості
Автогенерація таблиці
Всі дані беруться з html-elements.json, таблиця створюється динамічно.
Сортування
Підтримується сортування за:
- тегом
- статусом
- категорією
- описом
Пошук
Пошук працює по:
- назві тега
- опису
- категорії
Фільтри
Доступні статуси:
- All
- Standard
- Obsolete
- Historical
Темна / світла тема
Перемикається кнопкою.
Стан теми зберігається у localStorage.

## 📦 Формат JSON
Файл html-elements.json містить масив об’єктів:
{
  "html_elements": [
    {
      "tag": "a",
      "status": "standard",
      "category": "hyperlink",
      "description": "Гіперпосилання"
    }
  ]
}

## Поля:
|  |  | 
| tag |  | 
| status | standardobsoletehistorical | 
| category |  | 
| description |  | 

## 🛠 Використані технології
- HTML5
- CSS3
- JavaScript (Vanilla)
- JSON
Жодних сторонніх бібліотек.

## 🤝 Внесок
Pull‑requests вітаються.
Можна додавати:
- нові категорії;
- додаткові поля;
- приклади використання тегів;
- окремі сторінки для кожного елемента;
- розширення функціоналу таблиці.

## 📜 Ліцензія
MIT License — ви можете вільно використовувати, змінювати та поширювати цей проєкт.

## ⭐ Підтримка
Якщо цей довідник був корисним — поставте ⭐ на GitHub.
Це допоможе іншим знайти проєкт.
